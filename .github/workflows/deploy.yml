name: Deploy react
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}

      - run: docker build -t xxhjshamxx/multi-client ./microservices/client
      - run: docker build -t xxhjshamxx/multi-nginx ./microservices/nginx
      - run: docker build -t xxhjshamxx/multi-nginx ./microservices/server
      - run: docker build -t xxhjshamxx/multi-worker ./microservices/worker
      
      - run: docker push xxhjshamxx/multi-client
      - run: docker push xxhjshamxx/multi-nginx
      - run: docker push xxhjshamxx/multi-server
      - run: docker push xxhjshamxx/multi-worker

